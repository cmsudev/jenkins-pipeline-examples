pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello Intellipaattttttt...................................."'
                sh '''
                    echo "Multiline shell steps works too"
                    ls -lah
                '''
                git branch: 'DEV', url: 'https://github.com/cmsudev/jenkins-pipeline-examples.git'
            }
        }
    }
}
